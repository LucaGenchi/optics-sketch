// Public application release used by durable share links. This is separate
// from the scene JSON version in state.js: the JSON version describes the
// saved-data contract, while this release selects the frozen renderer.
export const APP_RELEASE = 'v1';
